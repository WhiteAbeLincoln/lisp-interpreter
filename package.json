{
  "scripts": {
    "start": "node build/index.js",
    "exec": "npm run build && npm start",
    "build": "tsc -p ./tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^10.12.9",
    "typescript": "^3.1.6"
  }
}
